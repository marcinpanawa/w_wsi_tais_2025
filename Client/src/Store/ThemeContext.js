import { createContext } from "react";
export const themeDefault = {
    theme : {
        headerClass: "container-fluid default",
        footerText: "Lorem Ipsum A.D. 2024"
    }
}
export const ThemeContext = createContext();